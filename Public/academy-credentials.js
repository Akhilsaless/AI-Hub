window.academyCredentialsRender=async function(){const host=document.querySelector('.wrap');if(!host)return;let c,p;try{[c,p]=await Promise.all([api('/api/academy/credentials'),api('/api/academy/passport')])}catch{return}const sec=document.createElement('section');sec.className='card';sec.style.marginTop='18px';const issued=c.issued||[],available=(c.available||[]).filter(x=>x.eligible&&!issued.some(i=>i.kind===x.kind&&i.credential_key===x.key));sec.innerHTML=`<div class="xp">SKILL PASSPORT & CREDENTIALS</div><h2>Proof of what you can actually do</h2><p class="meta">Credentials require mastery and capstone evidence. They are independently verifiable by credential ID.</p><div class="tracks">${issued.map(x=>`<div class="track"><div class="meta">VERIFIED CREDENTIAL</div><h3>🏅 ${x.title}</h3><p class="meta">${x.credential_id} · ${new Date(x.issued_at).toLocaleDateString()}</p><a class="btn" href="/verify.html?id=${encodeURIComponent(x.credential_id)}" target="_blank">Verify →</a></div>`).join('')||'<div class="track"><h3>No credentials issued yet</h3><p class="meta">Finish an eligible level or specialist path first.</p></div>'}</div>${available.length?`<h3>Ready to issue</h3><div class="formats">${available.map(x=>`<button class="btn primary" onclick="academyIssueCredential('${x.kind}','${x.key}')">Issue ${x.title}</button>`).join('')}</div>`:''}<hr style="border:0;border-top:1px solid var(--hub-line);margin:22px 0"><h3>Shareable learner profile</h3><p class="meta">Private by default. Publish only the credentials and portfolio projects you choose to make public.</p><input id="passportName" class="choice" placeholder="Display name" value="${(p.publicProfile?.display_name||'').replaceAll('"','&quot;')}"><input id="passportSlug" class="choice" placeholder="Profile URL name" value="${p.publicProfile?.slug||''}"><textarea id="passportBio" class="choice" rows="3" placeholder="Short professional bio">${p.publicProfile?.bio||''}</textarea><div class="formats"><button class="btn" onclick="academySaveProfile('private')">Keep private</button><button class="btn primary" onclick="academySaveProfile('public')">Publish profile</button>${p.sharePath?`<a class="btn" target="_blank" href="${p.sharePath}">View public profile →</a>`:''}</div><div id="passportMsg" class="meta" style="margin-top:10px"></div>`;host.appendChild(sec)};
window.academyIssueCredential=async function(kind,key){try{const r=await api('/api/academy/credentials',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({kind,key})});location.href='/verify.html?id='+encodeURIComponent(r.credential.credential_id)}catch(e){alert(e.message)}};
window.academySaveProfile=async function(visibility){try{const r=await api('/api/academy/passport',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({displayName:document.querySelector('#passportName').value,slug:document.querySelector('#passportSlug').value,bio:document.querySelector('#passportBio').value,visibility})});document.querySelector('#passportMsg').innerHTML=r.sharePath?`Published. <a href="${r.sharePath}" target="_blank">Open share profile</a>`:'Profile saved privately.'}catch(e){document.querySelector('#passportMsg').textContent=e.message}};
