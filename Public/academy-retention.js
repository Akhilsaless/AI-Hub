(()=>{const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));window.academyRetentionRender=async state=>{const r=state?.retention;if(!r)return;const host=document.createElement('section');host.className='card';host.style.marginTop='18px';const rev=r.revision||[],earned=(r.achievements||[]).filter(x=>x.earned);host.innerHTML=`<div class="xp">LEARNING RHYTHM</div><h2>Keep mastery alive</h2><p class="sub">XP rewards consistency, but only demonstrated mastery unlocks levels and credentials.</p><div class="tracks"><div class="track"><div class="meta">WEEKLY GOAL</div><h3>${r.weekly.completed}/${r.weekly.target} activities</h3><div class="bar"><div class="fill" style="width:${r.weekly.percent}%"></div></div><p class="meta">${r.weekly.remaining?`${r.weekly.remaining} remaining this week`:'Goal complete'}</p><button class="btn" id="editWeeklyGoal">Change goal</button></div><div class="track"><div class="meta">SPACED REVISION</div><h3>${rev.length} skill${rev.length===1?'':'s'} due</h3>${rev.slice(0,3).map(x=>`<button class="choice" onclick="window.academyPlay('${esc(x.lessonId)}')"><b>${esc(x.title)}</b><br><span class="meta">Last mastery ${x.score}% · review every ${x.interval} days</span></button>`).join('')||'<p class="meta">Nothing due. Keep progressing.</p>'}</div></div><h3>Achievements</h3><div class="nav">${(r.achievements||[]).map(x=>`<span class="pill" style="opacity:${x.earned?1:.45}">${x.earned?'✓':'○'} ${esc(x.title)}</span>`).join('')}</div>${earned.length?`<p class="meta">${earned.length} earned · badges never bypass mastery requirements.</p>`:''}`;const anchor=document.querySelector('#academyMissions')||document.querySelector('#specialistAcademies');anchor?.parentNode?.insertBefore(host,anchor);host.querySelector('#editWeeklyGoal')?.addEventListener('click',async()=>{const v=prompt('Activities per week (1–20)',String(r.weekly.target));if(v===null)return;const n=Math.max(1,Math.min(20,Number(v)||5));try{await fetch('/api/academy/goals',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({weeklyTarget:n})});location.reload()}catch(e){alert(e.message)}})}})();
