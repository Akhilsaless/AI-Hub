(()=>{const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(){const host=document.querySelector('#aiExplorer');if(!host)return;try{const r=await fetch('/api/academy/explore'),d=await r.json();host.innerHTML=`<div class="xp">AI MODELS & TOOLS EXPLORER</div><h2>Learn the landscape without memorizing hype</h2><p class="meta">Core lessons teach durable skills. This explorer holds changing vendor/model information and only labels live facts current after verified-source ingestion.</p><div class="tracks">${d.models.slice(0,11).map(m=>`<div class="track"><div class="meta">${esc(m.provider)}</div><h3>${esc(m.family)}</h3><p class="meta">Best for: ${m.bestFor.map(esc).join(' · ')}</p><div class="meta">${esc(m.access)}</div></div>`).join('')}</div><h3 style="margin-top:20px">Tool categories</h3><div class="tracks">${d.tools.map(t=>`<div class="track"><h3>${esc(t.title)}</h3><p class="meta">Learn: ${t.learn.map(esc).join(' · ')}</p><p class="meta">Includes ${t.examples.map(esc).join(', ')}</p></div>`).join('')}</div><h3 style="margin-top:20px">What's New in AI</h3>${d.updates?.length?d.updates.map(x=>`<div class="track" style="margin:8px 0"><b>${esc(x.title)}</b><p>${esc(x.summary)}</p><div class="meta">Why it matters: ${esc(x.why_it_matters)} · ${esc(x.source_name)}</div></div>`).join(''):`<p class="meta">No verified updates published yet. HOPE will not invent a “latest” feed without a trusted research source.</p>`}`}catch(e){host.innerHTML=`<p class="meta">Explorer unavailable: ${esc(e.message)}</p>`}}window.academyExplorerRender=load})();
