(()=>{const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function api(u,o){const r=await fetch(u,o),j=await r.json();if(!r.ok)throw Error(j.error||'Request failed');return j}async function load(){const d=await api('/api/academy/specialists'),root=document.querySelector('#specialistAcademies');if(!root)return;root.innerHTML=`<div class="xp">SPECIALIST ACADEMIES</div><h2>Turn core AI mastery into career-ready capability.</h2><p class="meta">Specializations unlock after the required core level. Each path has practical activities, assessments and a final specialist project.</p><div class="tracks">${d.tracks.map(t=>`<div class="track" style="opacity:${t.unlocked?1:.58}"><div class="meta">${t.unlocked?'🔓':'🔒'} requires ${esc(t.requiresLevel)} · ${t.status.completed}/${t.status.total} · avg ${t.status.average}%</div><h3>${esc(t.title)}</h3>${t.modules.map(m=>`<p class="meta"><b>${esc(m[1])}</b> — ${m[2].map(esc).join(' · ')}</p>`).join('')}<button class="btn" ${t.unlocked?'':`disabled`} onclick="window.openSpecialist('${esc(t.id)}')">${t.status.complete?'✓ Completed':'Open path →'}</button></div>`).join('')}</div>`;window._specialists=d.tracks}window.openSpecialist=id=>{const t=window._specialists.find(x=>x.id===id),modal=document.querySelector('#lesson'),box=document.querySelector('#lessonContent');modal.classList.add('open');box.innerHTML=`<div class="xp">SPECIALIST PATH</div><h1 style="font-size:40px">${esc(t.title)}</h1><p class="sub">Complete in order. Every activity requires practical evidence and 85% mastery.</p>${t.activities.map((a,i)=>`<button class="choice" onclick="window.specialistActivity('${esc(t.id)}','${esc(a.id)}','${esc(a.title)}','${esc(a.moduleTitle)}')">${a.completed?'✓':'○'} ${i+1}. ${esc(a.title)}<br><span class="meta">${esc(a.moduleTitle)} · ${esc(a.type)}${a.score?' · '+a.score+'%':''}</span></button>`).join('')}`};window.specialistActivity=(track,id,title,module)=>{const box=document.querySelector('#lessonContent');box.innerHTML=`<div class="xp">${esc(module)}</div><h1 style="font-size:40px">${esc(title)}</h1><p class="sub">Do this as a real task. Include the output, decisions you made, and how you verified the result.</p><textarea id="specialistWork" placeholder="Paste your practical work/evidence here" style="width:100%;min-height:220px;background:var(--hub-panel);border:1px solid var(--hub-line);border-radius:12px;color:var(--hub-text);padding:12px"></textarea><button class="btn primary" id="specialistSubmit">Evaluate specialist work</button><div id="specialistResult" class="card" style="margin-top:12px"></div>`;document.querySelector('#specialistSubmit').onclick=async()=>{const o=document.querySelector('#specialistResult');o.textContent='HOPE is evaluating…';try{const r=await api('/api/academy/specialists',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({trackId:track,activityId:id,work:document.querySelector('#specialistWork').value})});o.innerHTML=`<b>${r.score}% — ${r.passed?'PASS':'RETRY'}</b><p>${esc(r.feedback)}</p><p class="meta">Improve: ${esc(r.improve)}</p>${r.passed?'<button class="btn" onclick="location.reload()">Continue →</button>':''}`}catch(e){o.textContent=e.message}}};window.academyLoadSpecialists=load})();
