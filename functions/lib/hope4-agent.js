import {classifyIntent} from './hope3-orchestrator.js';
const WRITE=new Set(['gmail_draft','gmail_send','calendar_create','github_issue']);
export function actionFromText(input){const s=String(input||'').trim(),l=s.toLowerCase();if(/\b(search|find|look for|check)\b.*\b(mail|email|gmail)\b|\b(mail|email|gmail)\b.*\b(search|find|look for|check)\b/.test(l))return 'gmail_search';if(/\b(draft|write)\b.*\b(mail|email)\b/.test(l))return 'gmail_draft';if(/\b(send|email)\b.*\b(mail|email|message)\b|^email\b/.test(l))return 'gmail_send';if(/\b(calendar|schedule|events?)\b/.test(l)&&/\b(show|check|read|what|find)\b/.test(l))return 'calendar_read';if(/\b(schedule|create|book|add)\b.*\b(meeting|event|calendar)\b/.test(l))return 'calendar_create';if(/\b(github|repo|repository)\b/.test(l)&&/\b(check|read|show|inspect)\b/.test(l))return 'github_read';if(/\b(create|open|file)\b.*\b(issue\b)/.test(l))return 'github_issue';return null}
export function buildAgentPlan(input,capabilities=[]){const intent=classifyIntent(input),action=actionFromText(input),steps=[];if(action)steps.push({id:'understand',type:'reason',status:'ready'},{id:'validate',type:'capability',action,status:'ready'},{id:'execute',type:'tool',action,status:'blocked',requiresConfirmation:WRITE.has(action)},{id:'verify',type:'verify',action,status:'blocked'});else steps.push({id:'understand',type:'reason',status:'ready'},{id:'respond',type:'answer',status:'blocked'});return {version:'HOPE 4.0',objective:String(input||''),intent:intent.primary.name,action,steps,autonomous:!!action,capabilities,confirmationRequired:!!action&&WRITE.has(action)}}
export function actionNeedsConfirmation(action){return WRITE.has(action)}
