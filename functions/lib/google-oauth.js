import {encryptStoredKey,decryptStoredKey} from './vault.js';
export const GOOGLE_SCOPES=['openid','email','profile','https://www.googleapis.com/auth/gmail.modify','https://www.googleapis.com/auth/calendar.events','https://www.googleapis.com/auth/drive.file'];
export function origin(request){const u=new URL(request.url);return `${u.protocol}//${u.host}`}
export async function ensureGoogleTable(env){await env.DB.prepare(`CREATE TABLE IF NOT EXISTS tool_connectors(id TEXT PRIMARY KEY,label TEXT NOT NULL,key_cipher TEXT,iv TEXT,account TEXT,metadata TEXT,enabled INTEGER NOT NULL DEFAULT 1,updated_at TEXT NOT NULL)`).run()}
export async function loadGoogle(env){await ensureGoogleTable(env);const row=await env.DB.prepare(`SELECT * FROM tool_connectors WHERE id='google'`).first();if(!row)return {row:null,tokens:null,meta:{}};let tokens=null,meta={};try{tokens=JSON.parse(await decryptStoredKey(env,row)||'null')}catch{}try{meta=JSON.parse(row.metadata||'{}')}catch{}return {row,tokens,meta}}
export async function saveGoogle(env,tokens,account,meta={}){await ensureGoogleTable(env);const enc=await encryptStoredKey(env,JSON.stringify(tokens));const now=new Date().toISOString();await env.DB.prepare(`INSERT INTO tool_connectors(id,label,key_cipher,iv,account,metadata,enabled,updated_at) VALUES('google','Google Workspace',?,?,?,?,1,?) ON CONFLICT(id) DO UPDATE SET key_cipher=excluded.key_cipher,iv=excluded.iv,account=excluded.account,metadata=excluded.metadata,enabled=1,updated_at=excluded.updated_at`).bind(enc.cipher,enc.iv,account||'',JSON.stringify(meta),now).run()}
export async function accessToken(env){const {row,tokens,meta}=await loadGoogle(env);if(!row||!tokens)throw new Error('Google is not connected');if(tokens.access_token&&Number(tokens.expires_at||0)>Date.now()+60000)return {token:tokens.access_token,meta,row};if(!tokens.refresh_token)throw new Error('Google refresh token missing; reconnect Google');if(!env.GOOGLE_CLIENT_ID||!env.GOOGLE_CLIENT_SECRET)throw new Error('Google OAuth credentials missing');const body=new URLSearchParams({client_id:env.GOOGLE_CLIENT_ID,client_secret:env.GOOGLE_CLIENT_SECRET,refresh_token:tokens.refresh_token,grant_type:'refresh_token'});const r=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error_description||j.error||`Google token HTTP ${r.status}`);const next={...tokens,...j,refresh_token:tokens.refresh_token,expires_at:Date.now()+Number(j.expires_in||3600)*1000};await saveGoogle(env,next,row.account,meta);return {token:next.access_token,meta,row}}
export async function googleGet(env,url){const {token}=await accessToken(env);const r=await fetch(url,{headers:{Authorization:`Bearer ${token}`}}),j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j?.error?.message||j?.error_description||`Google API HTTP ${r.status}`);return j}
