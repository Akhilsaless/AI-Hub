import {lesson} from './academy.js';import {contentFor} from './academy-content.js';
const labTypes=new Set(['lab','battle','arena','interactive','mission','build','boss','practice','project','assessment','capstone']);
export function labFor(lessonId){const l=lesson(lessonId);if(!l)return null;const c=contentFor(lessonId);const enabled=labTypes.has(l.format);return {enabled,lessonId:l.id,title:l.title,levelId:l.levelId,type:l.format,objective:c?.objective||'',brief:c?.practice?.instruction||`Apply ${l.title} to a realistic task.`,successCriteria:c?.mastery?.checks||[],requiredEvidence:c?.practice?.evidence||[],compareModels:['arena','battle'].includes(l.format),attemptsRecommended:l.levelId==='advanced'?3:2};}
export function rubricFor(l){const x=typeof l==='string'?lesson(l):l;if(!x)return null;const base=[['correctness',x.levelId==='beginner'?35:30,'Uses the concept correctly'],['application',25,'Applies it to the stated scenario'],['verification',x.levelId==='advanced'?25:20,'Checks assumptions, outputs or failure modes'],['explanation',20,'Explains decisions and trade-offs']];if(x.levelId==='advanced')base.push(['production',10,'Addresses reliability, cost, security or observability']);return base;}
export function scoreRubric({lessonId,ratings={}}={}){const l=lesson(lessonId),rubric=rubricFor(l);if(!l||!rubric)return null;let score=0,weight=0;const details=rubric.map(([id,w,label])=>{const r=Math.max(0,Math.min(5,Number(ratings[id]||0)));score+=r/5*w;weight+=w;return{id,label,weight:w,rating:r}});return {score:Math.round(score/weight*100),details,passScore:l.levelId==='beginner'?80:l.levelId==='intermediate'?85:88};}
export function retryPlan({lessonId,score=0,attempt=1}={}){const l=lesson(lessonId);if(!l)return null;return {attempt:attempt+1,change:score<60?'Use a simpler scenario and one guided hint.':score<80?'Keep the scenario but target the weakest rubric criterion.':'Increase ambiguity or constraints so the learner must transfer the skill.',mustDiffer:true};}
