import {executeHopeGateway} from './hope-gateway.js';
export async function ensureHyvoraMarketingSchema(env){const statements=[`CREATE TABLE IF NOT EXISTS marketing_brand_profiles(user_id TEXT PRIMARY KEY,brand_name TEXT NOT NULL DEFAULT '',description TEXT NOT NULL DEFAULT '',audience TEXT NOT NULL DEFAULT '',offer TEXT NOT NULL DEFAULT '',voice TEXT NOT NULL DEFAULT 'clear, useful, credible',goals TEXT NOT NULL DEFAULT '[]',platforms TEXT NOT NULL DEFAULT '[]',guardrails TEXT NOT NULL DEFAULT '[]',updated_at TEXT NOT NULL)`,`CREATE TABLE IF NOT EXISTS marketing_campaigns(id TEXT PRIMARY KEY,user_id TEXT NOT NULL,name TEXT NOT NULL,objective TEXT NOT NULL,platforms TEXT NOT NULL DEFAULT '[]',status TEXT NOT NULL DEFAULT 'draft',approval_mode TEXT NOT NULL DEFAULT 'before_publish',strategy TEXT NOT NULL DEFAULT '{}',schedule TEXT NOT NULL DEFAULT '{}',created_at TEXT NOT NULL,updated_at TEXT NOT NULL)`,`CREATE TABLE IF NOT EXISTS marketing_assets(id TEXT PRIMARY KEY,campaign_id TEXT NOT NULL,user_id TEXT NOT NULL,platform TEXT NOT NULL,asset_type TEXT NOT NULL,content TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'draft',metadata TEXT NOT NULL DEFAULT '{}',created_at TEXT NOT NULL,updated_at TEXT NOT NULL)`,`CREATE TABLE IF NOT EXISTS marketing_publication_jobs(id TEXT PRIMARY KEY,campaign_id TEXT NOT NULL,asset_id TEXT NOT NULL,user_id TEXT NOT NULL,platform TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'awaiting_approval',scheduled_for TEXT,result TEXT,error TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL)`,`CREATE TABLE IF NOT EXISTS marketing_metrics(id INTEGER PRIMARY KEY AUTOINCREMENT,user_id TEXT NOT NULL,campaign_id TEXT,asset_id TEXT,platform TEXT NOT NULL,metric TEXT NOT NULL,value REAL NOT NULL DEFAULT 0,observed_at TEXT NOT NULL,source TEXT NOT NULL DEFAULT 'manual')`,`CREATE INDEX IF NOT EXISTS idx_marketing_campaigns_user ON marketing_campaigns(user_id,updated_at)`,`CREATE INDEX IF NOT EXISTS idx_marketing_assets_campaign ON marketing_assets(user_id,campaign_id,updated_at)`,`CREATE INDEX IF NOT EXISTS idx_marketing_publication_user ON marketing_publication_jobs(user_id,status,scheduled_for)`,`CREATE INDEX IF NOT EXISTS idx_marketing_metrics_campaign ON marketing_metrics(user_id,campaign_id,observed_at)`];for(const sql of statements)await env.DB.prepare(sql).run()}
export const parseJson=(value,fallback=[])=>{try{return JSON.parse(value)}catch{return fallback}};export const id=(prefix='mkt')=>`${prefix}_${crypto.randomUUID()}`;export const now=()=>new Date().toISOString();export const cleanList=value=>Array.from(new Set((Array.isArray(value)?value:[]).map(v=>String(v||'').trim()).filter(Boolean))).slice(0,20);
export function fallbackStrategy({brand,objective,platforms}){const audience=brand?.audience||'the intended audience',offer=brand?.offer||brand?.description||'the brand offer';return{positioning:`Show how ${offer} helps ${audience} achieve ${objective}.`,pillars:['Educate with useful proof','Demonstrate the product or process','Build trust with outcomes and stories','Convert with one clear next action'],cadence:platforms.reduce((acc,p)=>({...acc,[p]:'3-5 quality posts per week, adjusted after analytics'}),{}),experiment:'Test two hooks for the same core idea and keep the winner based on retention/engagement, not vanity impressions.',safety:'Publishing remains approval-gated until a platform connection is verified.'}}
export async function listHyvoraCampaigns(env,userId){await ensureHyvoraMarketingSchema(env);const q=await env.DB.prepare(`SELECT id,name,objective,platforms,status,approval_mode,strategy,schedule,created_at,updated_at FROM marketing_campaigns WHERE user_id=? ORDER BY updated_at DESC LIMIT 50`).bind(userId).all();return(q.results||[]).map(r=>({...r,platforms:parseJson(r.platforms),strategy:parseJson(r.strategy,{}),schedule:parseJson(r.schedule,{})}))}
export async function createHyvoraCampaign(env,{user,name,objective,platforms}={}){await ensureHyvoraMarketingSchema(env);const n=String(name||'').trim().slice(0,120),o=String(objective||'').trim().slice(0,1000),ps=cleanList(platforms);if(!user?.id)throw Error('Authenticated user required');if(!n||!o||!ps.length)throw Error('Campaign name, objective and at least one platform are required');const row=await env.DB.prepare(`SELECT * FROM marketing_brand_profiles WHERE user_id=?`).bind(user.id).first();if(!row)throw Error('Create your Brand Brain before starting a campaign');const brand={brandName:row.brand_name,description:row.description,audience:row.audience,offer:row.offer,voice:row.voice,goals:parseJson(row.goals),guardrails:parseJson(row.guardrails)};const prompt=`Create a concise social marketing campaign strategy as strict JSON with keys positioning, pillars (array), cadence (object), experiment, safety. Brand: ${JSON.stringify(brand)}. Objective: ${o}. Platforms: ${ps.join(', ')}. Do not claim real publishing, live analytics, or connected accounts. Respect the brand guardrails.`;const ai=await executeHopeGateway(env,{user,prompt,intent:'strategy',system:'You are HYVORA Marketing Strategy. Return useful, grounded strategy. Never fabricate metrics, platform connections, publishing success, or customer results.',messages:[{role:'user',content:prompt}]});let strategy;try{strategy=JSON.parse(String(ai.content||ai.text||ai.output||'').replace(/^```json\s*|\s*```$/g,''))}catch{strategy=fallbackStrategy({brand,objective:o,platforms:ps})}const campaignId=id('camp'),stamp=now();await env.DB.prepare(`INSERT INTO marketing_campaigns(id,user_id,name,objective,platforms,status,approval_mode,strategy,schedule,created_at,updated_at) VALUES(?,?,?,?,?,'draft','before_publish',?,'{}',?,?)`).bind(campaignId,user.id,n,o,JSON.stringify(ps),JSON.stringify(strategy),stamp,stamp).run();return{id:campaignId,name:n,objective:o,platforms:ps,status:'draft',approvalMode:'before_publish',strategy,aiUsed:Boolean(ai.ok)}}
export async function readHyvoraMetrics(env,userId,campaignId=null){await ensureHyvoraMarketingSchema(env);let q;if(campaignId){const own=await env.DB.prepare(`SELECT id FROM marketing_campaigns WHERE id=? AND user_id=?`).bind(campaignId,userId).first();if(!own)throw Error('Campaign not found');q=await env.DB.prepare(`SELECT campaign_id,platform,metric,SUM(value) value,MAX(observed_at) latest FROM marketing_metrics WHERE user_id=? AND campaign_id=? GROUP BY campaign_id,platform,metric ORDER BY latest DESC LIMIT 250`).bind(userId,campaignId).all()}else q=await env.DB.prepare(`SELECT campaign_id,platform,metric,SUM(value) value,MAX(observed_at) latest FROM marketing_metrics WHERE user_id=? GROUP BY campaign_id,platform,metric ORDER BY latest DESC LIMIT 250`).bind(userId).all();return q.results||[]}
