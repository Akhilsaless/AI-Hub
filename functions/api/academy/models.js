import {requireUser} from '../../lib/user-auth.js';import {modelExplorer} from '../../lib/academy-model-explorer.js';
const json=v=>new Response(JSON.stringify(v),{headers:{'content-type':'application/json','cache-control':'no-store'}});
export async function onRequestGet({request,env}){const a=await requireUser(request,env);if(a.response)return a.response;const u=new URL(request.url);return json({ok:true,models:modelExplorer({q:u.searchParams.get('q')||'',provider:u.searchParams.get('provider')||''}),policy:{coreCurriculum:'Teaches durable model-selection concepts and does not depend on temporary model rankings.',explorer:'Current model names, capabilities, access, pricing and limits belong here and must be refreshed from an approved research source.',lastVerified:null,liveResearchConfigured:false}});}
