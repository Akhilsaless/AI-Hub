import {requireUser} from '../../lib/user-auth.js';import {modelExplorer} from '../../lib/academy-model-explorer.js';import {toolExplorer,TOOL_CATEGORIES} from '../../lib/academy-tools-explorer.js';
const json=v=>new Response(JSON.stringify(v),{headers:{'content-type':'application/json','cache-control':'no-store'}});
export async function onRequestGet({request,env}){const a=await requireUser(request,env);if(a.response)return a.response;const u=new URL(request.url),q=u.searchParams.get('q')||'',category=u.searchParams.get('category')||'';return json({ok:true,models:modelExplorer({q}),tools:toolExplorer({q,category}),categories:TOOL_CATEGORIES,policy:{durableCurriculum:'Core Academy progression never depends on current vendor rankings, prices or temporary free tiers.',dynamicExplorer:'Model/tool availability, capabilities, pricing and limits must be verified from approved current sources before being labelled current.',updates:'AI news micro-lessons are optional and never block core progression.',liveResearchConfigured:false}})}
