import {requireUser} from '../../lib/user-auth.js';import {TRACKS,allLessons,dailyMission,levelFor} from '../../lib/academy.js';import {retentionSnapshot,battleFor} from '../../lib/academy-retention.js';
const json=(v,s=200)=>new Response(JSON.stringify(v),{status:s,headers:{'content-type':'application/json','cache-control':'no-store'}});
async function ensure(env){await env.DB.batch([env.DB.prepare(`CREATE TABLE IF NOT EXISTS academy_profiles(user_id TEXT PRIMARY KEY,xp INTEGER NOT NULL DEFAULT 0,streak INTEGER NOT NULL DEFAULT 0,last_active TEXT,skill_scores TEXT NOT NULL DEFAULT '{}',updated_at TEXT NOT NULL)`),env.DB.prepare(`CREATE TABLE IF NOT EXISTS academy_progress(user_id TEXT NOT NULL,lesson_id TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'started',score INTEGER NOT NULL DEFAULT 0,xp_earned INTEGER NOT NULL DEFAULT 0,completed_at TEXT,updated_at TEXT NOT NULL,PRIMARY KEY(user_id,lesson_id))`),env.DB.prepare(`CREATE TABLE IF NOT EXISTS academy_missions(user_id TEXT NOT NULL,mission_id TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'active',score INTEGER NOT NULL DEFAULT 0,xp_earned INTEGER NOT NULL DEFAULT 0,updated_at TEXT NOT NULL,PRIMARY KEY(user_id,mission_id))`)])}
export async function onRequestGet({request,env}){const a=await requireUser(request,env);if(a.response)return a.response;await ensure(env);const u=a.user,now=new Date().toISOString(),p=await env.DB.prepare(`SELECT * FROM academy_profiles WHERE user_id=?`).bind(u.id).first()||{xp:0,streak:0,last_active:null,skill_scores:'{}'},r=await env.DB.prepare(`SELECT * FROM academy_progress WHERE user_id=?`).bind(u.id).all(),progress=r.results||[],done=progress.filter(x=>x.status==='completed'),completed=new Set(done.map(x=>x.lesson_id)),skills=JSON.parse(p.skill_scores||'{}'),tracks=TRACKS.map(t=>({...t,total:t.lessons.length,completed:t.lessons.filter(l=>completed.has(l[0])).length,lessons:t.lessons.map(l=>({id:l[0],title:l[1],format:l[2],completed:completed.has(l[0]),score:Number(progress.find(x=>x.lesson_id===l[0])?.score||0)}))})),profile={xp:Number(p.xp||0),level:levelFor(Number(p.xp||0)),streak:Number(p.streak||0),lastActive:p.last_active,skills,completedLessons:done.length,totalLessons:allLessons().length},retention=retentionSnapshot({profile:p,progress,skills});return json({ok:true,user:u,profile,dailyMission:dailyMission(),retention,nextBattle:battleFor(retention.quest.lessonId),tracks,plans:{current:u.plan,available:['free','pro','builder','teams']},serverTime:now})}