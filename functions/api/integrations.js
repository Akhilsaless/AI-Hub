import {requireOwner} from '../lib/auth.js';import {encryptStoredKey} from '../lib/vault.js';const json=(d,s=200)=>new Response(JSON.stringify(d),{status:s,headers:{'content-type':'application/json','cache-control':'no-store'}});async function ensureSchema(env){await env.DB.batch([env.DB.prepare(`CREATE TABLE IF NOT EXISTS integrations(id TEXT PRIMARY KEY,provider TEXT NOT NULL,label TEXT,endpoint TEXT,model TEXT,key_cipher TEXT,iv TEXT,verified_free INTEGER NOT NULL DEFAULT 0,enabled INTEGER NOT NULL DEFAULT 0,updated_at TEXT NOT NULL)`),env.DB.prepare(`CREATE TABLE IF NOT EXISTS products(id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL UNIQUE,status TEXT NOT NULL DEFAULT 'active',created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`),env.DB.prepare(`CREATE TABLE IF NOT EXISTS request_logs(id INTEGER PRIMARY KEY AUTOINCREMENT,product TEXT,provider TEXT,model TEXT,success INTEGER NOT NULL DEFAULT 0,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`)])}export async function onRequestGet({request,env}){const denied=await requireOwner(request,env);if(denied)return denied;if(!env.DB)return json({ok:false,error:'DB binding missing'},500);try{await ensureSchema(env);const rows=await env.DB.prepare(`SELECT id,provider,label,endpoint,model,verified_free,enabled,updated_at,CASE WHEN key_cipher IS NOT NULL THEN 1 ELSE 0 END has_credential FROM integrations ORDER BY provider`).all();return json({ok:true,items:rows.results||[]})}catch(e){return json({ok:false,error:String(e?.message||e)},500)}}export async function onRequestPost({request,env}){const denied=await requireOwner(request,env);if(denied)return denied;if(!env.DB)return json({ok:false,error:'DB binding missing'},500);if(!env.HUB_MASTER_KEY)return json({ok:false,error:'HUB_MASTER_KEY missing'},500);try{await ensureSchema(env);const input=await request.json(),provider=String(input.provider||'').trim();if(!provider)return json({ok:false,error:'provider is required'},400);const id=String(input.id||provider).trim();let keyCipher=null,iv=null;let secret=String(input.apiKey||'').trim();if(input.credentials&&typeof input.credentials==='object')secret=JSON.stringify(input.credentials);if(secret){const encrypted=await encryptStoredKey(env,secret);keyCipher=encrypted.cipher;iv=encrypted.iv}await env.DB.prepare(`INSERT INTO integrations(id,provider,label,endpoint,model,key_cipher,iv,verified_free,enabled,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?) ON CONFLICT(id) DO UPDATE SET provider=excluded.provider,label=excluded.label,endpoint=excluded.endpoint,model=excluded.model,key_cipher=COALESCE(excluded.key_cipher,integrations.key_cipher),iv=COALESCE(excluded.iv,integrations.iv),verified_free=excluded.verified_free,enabled=excluded.enabled,updated_at=excluded.updated_at`).bind(id,provider,String(input.label||provider),String(input.endpoint||''),String(input.model||''),keyCipher,iv,input.verifiedFree?1:0,input.enabled?1:0,new Date().toISOString()).run();return json({ok:true,id,credentialStored:Boolean(secret)})}catch(e){return json({ok:false,error:String(e?.message||e)},500)}}export async function onRequestDelete({request,env}){const denied=await requireOwner(request,env);if(denied)return denied;if(!env.DB)return json({ok:false,error:'DB binding missing'},500);try{await ensureSchema(env);const id=new URL(request.url).searchParams.get('id');if(!id)return json({ok:false,error:'id is required'},400);await env.DB.prepare('DELETE FROM integrations WHERE id=?').bind(id).run();return json({ok:true})}catch(e){return json({ok:false,error:String(e?.message||e)},500)}}