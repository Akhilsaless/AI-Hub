import {requireUser} from '../../../../lib/user-auth.js';import {makeOAuthState} from '../../../../lib/oauth-state.js';
export async function onRequestGet({request,env}){const a=await requireUser(request,env);if(a.response)return a.response;if(!env.GITHUB_CLIENT_ID)return new Response('GitHub OAuth is not configured',{status:503});const state=await makeOAuthState(env,a.user.id,'github'),q=new URLSearchParams({client_id:env.GITHUB_CLIENT_ID,redirect_uri:new URL(request.url).origin+'/api/user/connect/github/callback',scope:'repo user:email',state});return Response.redirect('https://github.com/login/oauth/authorize?'+q,302)}
