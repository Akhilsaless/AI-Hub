import {requireUser} from '../../../../lib/user-auth.js';import {makeOAuthState} from '../../../../lib/oauth-state.js';
export async function onRequestGet({request,env}){const a=await requireUser(request,env);if(a.response)return a.response;if(!env.GOOGLE_CLIENT_ID)return new Response('Google OAuth is not configured',{status:503});const u=new URL(request.url),redirect=`${u.origin}/api/user/connect/google/callback`,state=await makeOAuthState(env,a.user.id,'google'),q=new URLSearchParams({client_id:env.GOOGLE_CLIENT_ID,redirect_uri:redirect,response_type:'code',access_type:'offline',prompt:'consent',include_granted_scopes:'true',state,scope:'openid email https://www.googleapis.com/auth/gmail.readonly https://www.googleapis.com/auth/gmail.compose https://www.googleapis.com/auth/gmail.send https://www.googleapis.com/auth/calendar.readonly https://www.googleapis.com/auth/calendar.events'});return Response.redirect('https://accounts.google.com/o/oauth2/v2/auth?'+q,302)}
