import {requireUser} from '../../../../lib/user-auth.js';
export async function onRequestGet({request,env}){const a=await requireUser(request,env);if(a.response)return a.response;return new Response(JSON.stringify({ok:false,error:'HOPE chooses its AI automatically. Provider connections are managed by the Owner.'}),{status:403,headers:{'content-type':'application/json','cache-control':'no-store'}})}
