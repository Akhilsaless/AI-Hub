import {requireUser} from '../../../lib/user-auth.js';
import {classifyIntent} from '../../../lib/hope3-orchestrator.js';
import {buildAgentPlan,actionNeedsConfirmation,missingActionFields} from '../../../lib/hope4-agent.js';
import {ACTIONS} from '../../../lib/hope4-executor.js';
const json=(v,s=200)=>new Response(JSON.stringify(v,null,2),{status:s,headers:{'content-type':'application/json','cache-control':'no-store'}});
export async function onRequestGet({request,env}){const a=await requireUser(request,env);if(a.response)return a.response;const checks=[],check=(name,ok,detail='')=>checks.push({name,ok:!!ok,detail});try{check('authenticated-user',!!a.user?.id,a.user?.isOwner?'owner':'customer');check('general-chat',classifyIntent('Explain why the sky is blue').primary.name!=='memory');check('research-routing',!!classifyIntent('Research current AI model news').primary.name);const gs=buildAgentPlan('Search my Gmail for invoice emails',['gmail']),gd=buildAgentPlan('Send an email to sam@example.com subject: Status saying Project is ready',['gmail']),cr=buildAgentPlan('Show my calendar events today',['calendar']),cc=buildAgentPlan('Create a calendar event called Review 2026-08-14T10:00:00Z 2026-08-14T10:30:00Z',['calendar']),gr=buildAgentPlan('Read my GitHub repository Akhilsaless/AI-Hub',['repos']),gi=buildAgentPlan('Create a GitHub issue "Login bug" in Akhilsaless/AI-Hub',['issues']);check('gmail-search-detected',gs.action==='gmail_search'&&gs.autonomous);check('gmail-send-payload',gd.action==='gmail_send'&&gd.payload.to==='sam@example.com'&&missingActionFields(gd.action,gd.payload).length===0);check('gmail-write-confirmation',gd.confirmationRequired&&actionNeedsConfirmation(gd.action));check('calendar-read-detected',cr.action==='calendar_read'&&!cr.confirmationRequired);check('calendar-create-payload',cc.action==='calendar_create'&&missingActionFields(cc.action,cc.payload).length===0);check('calendar-write-confirmation',cc.confirmationRequired);check('github-read-payload',gr.action==='github_read'&&gr.payload.repo==='Akhilsaless/AI-Hub');check('github-issue-payload',gi.action==='github_issue'&&gi.payload.repo==='Akhilsaless/AI-Hub'&&!!gi.payload.title);check('github-write-confirmation',gi.confirmationRequired);check('executor-contracts',['gmail_search','gmail_draft','gmail_send','calendar_read','calendar_create','github_read','github_issue'].every(x=>!!ACTIONS[x]));check('lifecycle',[gs,gd,cr,cc,gr,gi].every(p=>['understand','validate','execute','verify'].every(id=>p.steps.some(s=>s.id===id))));const incomplete=buildAgentPlan('Send an email',['gmail']);check('missing-input-guard',missingActionFields(incomplete.action,incomplete.payload).length>0);await env.DB.prepare(`CREATE TABLE IF NOT EXISTS user_hope_threads(id TEXT NOT NULL,user_id TEXT NOT NULL,title TEXT NOT NULL,created_at TEXT NOT NULL,updated_at TEXT NOT NULL,PRIMARY KEY(user_id,id))`).run();await env.DB.prepare(`CREATE TABLE IF NOT EXISTS user_hope_messages(id INTEGER PRIMARY KEY AUTOINCREMENT,user_id TEXT NOT NULL,thread_id TEXT NOT NULL DEFAULT 'legacy',role TEXT NOT NULL,content TEXT NOT NULL,attachments TEXT NOT NULL DEFAULT '[]',created_at TEXT NOT NULL)`).run();await env.DB.prepare(`CREATE TABLE IF NOT EXISTS user_hope_memory(id INTEGER PRIMARY KEY AUTOINCREMENT,user_id TEXT NOT NULL,content TEXT NOT NULL,importance INTEGER NOT NULL DEFAULT 5,created_at TEXT NOT NULL)`).run();await env.DB.prepare(`CREATE TABLE IF NOT EXISTS user_hope_agent_runs(id TEXT PRIMARY KEY,user_id TEXT NOT NULL,objective TEXT NOT NULL,plan TEXT NOT NULL,status TEXT NOT NULL,current_step TEXT,result TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL)`).run();check('conversation-storage',true);check('memory-storage',true);check('agent-audit-storage',true);const failed=checks.filter(x=>!x.ok);return json({ok:!failed.length,release:'HOPE Phase 4 final gate',summary:{passed:checks.length-failed.length,total:checks.length,failed:failed.length},checks,liveConnectorTests:'Run only with the signed-in user’s own connected Google/GitHub accounts; this smoke endpoint intentionally does not send email, create events, or create issues.',manualProductionChecks:['Mood controls only under +','Very Funny/Happy/Angry visibly affect safe casual replies','Confirmation card continues the same agent run','Cancel performs no external side effect','Read action shows verified result','Write action shows verified result only after confirmation','Voice output works when enabled','Conversation delete preserves memory','Mobile composer remains usable'],failed:failed.map(x=>x.name)},failed.length?503:200)}catch(e){return json({ok:false,release:'HOPE Phase 4 final gate',checks,error:String(e?.message||e)},500)}}
