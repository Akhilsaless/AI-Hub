import {CONNECTOR_CATALOG,connector} from '../../lib/user-connectors.js';
import {PLANS} from '../../lib/saas-entitlements.js';
const json=v=>new Response(JSON.stringify(v),{headers:{'content-type':'application/json','cache-control':'no-store'}});
export async function onRequestGet(){const ids=CONNECTOR_CATALOG.map(x=>x.id),checks={uniqueIds:new Set(ids).size===ids.length,googleAvailable:!!connector('google'),githubAvailable:!!connector('github'),freeHasGoogle:connector('google')?.plans.includes('free')===true,githubNotSharedByDefault:connector('github')?.plans.includes('free')===false,allHaveCapabilities:CONNECTOR_CATALOG.every(x=>x.capabilities?.length),allPlansValid:CONNECTOR_CATALOG.every(x=>x.plans.every(p=>!!PLANS[p])),futureExplicit:CONNECTOR_CATALOG.filter(x=>x.auth==='oauth_future').length>=1};return json({ok:Object.values(checks).every(Boolean),checks,catalogSize:CONNECTOR_CATALOG.length,note:'Structural audit only. Live OAuth requires deployed provider credentials and callback tests.'})}