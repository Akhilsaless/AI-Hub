import {requireOwner} from '../../lib/auth.js';
import {executeTask} from '../../lib/task-executor.js';
import {ensureAutomationSchema,nextAfter} from '../../lib/automation-schedule.js';
const json=(v,s=200)=>new Response(JSON.stringify(v),{status:s,headers:{'content-type':'application/json','cache-control':'no-store'}});
async function authorize(request,env){const secret=env.AUTOMATION_RUNNER_SECRET;if(secret&&request.headers.get('authorization')===`Bearer ${secret}`)return null;return requireOwner(request,env)}
export async function onRequestPost({request,env}){const denied=await authorize(request,env);if(denied)return denied;await ensureAutomationSchema(env);const now=new Date();const q=await env.DB.prepare(`SELECT * FROM agent_tasks WHERE enabled=1 AND status IN ('pending','approved') AND next_run_at IS NOT NULL AND next_run_at<=? ORDER BY next_run_at ASC LIMIT 20`).bind(now.toISOString()).all();const results=[];for(const task of q.results||[]){if(task.requires_approval&&task.status!=='approved'){results.push({id:task.id,status:'awaiting_approval'});continue}const started=new Date().toISOString();const rr=await env.DB.prepare(`INSERT INTO automation_runs(task_id,status,started_at) VALUES(?,'running',?)`).bind(task.id,started).run(),runId=rr.meta?.last_row_id;await env.DB.prepare(`UPDATE agent_tasks SET status='running',updated_at=? WHERE id=? AND status IN ('pending','approved')`).bind(started,task.id).run();try{const result=await executeTask(env,task);const finished=new Date(),next=nextAfter(task.recurrence,finished),status=next?(task.requires_approval?'approved':'pending'):'completed';await env.DB.prepare(`UPDATE automation_runs SET status='completed',finished_at=?,result=?,verified=1 WHERE id=?`).bind(finished.toISOString(),JSON.stringify(result),runId).run();await env.DB.prepare(`UPDATE agent_tasks SET status=?,result=?,last_run_at=?,next_run_at=?,run_count=COALESCE(run_count,0)+1,updated_at=? WHERE id=?`).bind(status,JSON.stringify(result),finished.toISOString(),next,finished.toISOString(),task.id).run();results.push({id:task.id,status,verified:true,nextRunAt:next,result})}catch(e){const finished=new Date().toISOString(),error=String(e?.message||e);await env.DB.prepare(`UPDATE automation_runs SET status='failed',finished_at=?,error=?,verified=0 WHERE id=?`).bind(finished,error,runId).run();await env.DB.prepare(`UPDATE agent_tasks SET status='failed',result=?,last_run_at=?,failure_count=COALESCE(failure_count,0)+1,updated_at=? WHERE id=?`).bind(JSON.stringify({verified:false,error}),finished,finished,task.id).run();results.push({id:task.id,status:'failed',verified:false,error})}}return json({ok:true,checked:(q.results||[]).length,results,at:new Date().toISOString()})}
