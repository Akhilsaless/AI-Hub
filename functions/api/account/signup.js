import {ensureUsers,hashPassword,createUserSession,userCookie} from '../../lib/user-auth.js';
const json=(v,s=200,h={})=>new Response(JSON.stringify(v),{status:s,headers:{'content-type':'application/json','cache-control':'no-store',...h}});
export async function onRequestPost({request,env}){try{await ensureUsers(env);const b=await request.json(),email=String(b.email||'').trim().toLowerCase(),name=String(b.name||'').trim().slice(0,80),password=String(b.password||'');if(!/^\S+@\S+\.\S+$/.test(email))return json({ok:false,error:'Valid email required'},400);if(name.length<2)return json({ok:false,error:'Name required'},400);if(password.length<8)return json({ok:false,error:'Password must be at least 8 characters'},400);if(await env.DB.prepare(`SELECT id FROM app_users WHERE email=?`).bind(email).first())return json({ok:false,error:'Account already exists'},409);const id=`usr_${crypto.randomUUID()}`,p=await hashPassword(password),now=new Date().toISOString();await env.DB.prepare(`INSERT INTO app_users(id,email,name,password_hash,password_salt,plan,status,created_at,updated_at) VALUES(?,?,?,?,?,'free','active',?,?)`).bind(id,email,name,p.hash,p.salt,now,now).run();const token=await createUserSession(env,{id,email,plan:'free'});return json({ok:true,user:{id,email,name,plan:'free'}},201,{'set-cookie':userCookie(token)})}catch(e){return json({ok:false,error:String(e?.message||e)},500)}}