import assert from 'node:assert/strict';import fs from 'node:fs';import {credentialId,levelCredentialEligibility,specialistCredentialEligibility,skillPassport} from '../functions/lib/academy-credentials.js';import {levelLessons} from '../functions/lib/academy.js';import {SPECIALISTS,specialistActivities} from '../functions/lib/academy-specialists.js';
const bp=levelLessons('beginner').map(x=>({lesson_id:x.id,status:'completed',score:90}));assert.equal(levelCredentialEligibility('beginner',bp).eligible,true);assert.equal(levelCredentialEligibility('intermediate',bp).eligible,false);assert.match(credentialId('user','level','beginner','2026-08-21T00:00:00Z'),/^HOPE-L-[A-Z0-9]{7,}$/);const t=SPECIALISTS[0],sw=specialistActivities(t).map(x=>({track_id:t.id,activity_id:x.id,status:'passed',score:90}));assert.equal(specialistCredentialEligibility(t.id,sw).eligible,true);const pass=skillPassport({profile:{xp:1000,level:3,skills:{Prompting:92}},portfolio:[{id:'x',title:'Project',eligible:true,average:90,skills:['prompting']}],credentials:[{credential_id:'HOPE-L-ABCDEFG',title:'Beginner',kind:'level',credential_key:'beginner',issued_at:'now'}]});assert.equal(pass.skills[0].band,'Expert evidence');assert.equal(pass.projects.length,1);for(const f of ['functions/api/academy/credentials.js','functions/api/academy/verify.js','functions/api/academy/passport.js','functions/api/academy/public-profile.js','Public/academy-credentials.js','Public/verify.html','Public/profile.html'])assert.ok(fs.existsSync(f),`missing ${f}`);const learn=fs.readFileSync('Public/learn.html','utf8');assert.match(learn,/academy-credentials\.js/);assert.match(learn,/academyCredentialsRender/);const pub=fs.readFileSync('functions/api/academy/public-profile.js','utf8');assert.match(pub,/visibility='public'/);console.log(JSON.stringify({ok:true,phase:7,credentials:'evidence-backed',publicProfile:'opt-in'}));