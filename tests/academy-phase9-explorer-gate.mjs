import assert from 'node:assert/strict';import {MODEL_EXPLORER,modelExplorer} from '../functions/lib/academy-model-explorer.js';import {TOOL_CATEGORIES,toolExplorer,newsToMicroLesson} from '../functions/lib/academy-tools-explorer.js';
assert.ok(MODEL_EXPLORER.length>=10);for(const m of MODEL_EXPLORER){assert.ok(m.official.startsWith('https://'));assert.equal(m.dynamic,true);assert.ok(m.bestFor.length>=2)}assert.ok(modelExplorer({q:'coding'}).length>=3);assert.ok(TOOL_CATEGORIES.length>=7);assert.ok(toolExplorer({category:'automation-agents'}).length===1);const micro=newsToMicroLesson({id:'x',title:'New model capability',whyItMatters:'Changes a workflow'});assert.equal(micro.requiredForProgression,false);assert.equal(micro.sourceRequired,true);assert.ok(micro.sections.includes('Verify the claim'));console.log(JSON.stringify({ok:true,models:MODEL_EXPLORER.length,categories:TOOL_CATEGORIES.length,microLessonsOptional:true}));
