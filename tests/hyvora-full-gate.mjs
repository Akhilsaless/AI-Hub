import fs from'node:fs';import assert from'node:assert/strict';const r=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),'utf8');const social=r('functions/lib/hyvora-social.js'),metrics=r('functions/api/marketing/metrics.js'),repurpose=r('functions/api/marketing/repurpose.js'),calendar=r('functions/api/marketing/calendar.js'),approvals=r('functions/api/marketing/approvals.js');for(const p of ['youtube','instagram','facebook'])assert.ok(social.includes(p));assert.ok(social.includes('publishEnabled'));assert.ok(social.includes('adapter_requires_media'));assert.ok(metrics.includes('marketing_metrics'));assert.ok(metrics.includes('executeHopeGateway'));assert.ok(metrics.includes('insufficient_data'));assert.ok(repurpose.includes('sourceAssetId'));assert.ok(repurpose.includes('executeHopeGateway'));assert.ok(calendar.includes('marketing_publication_jobs'));assert.ok(approvals.includes('approved_waiting_connection'));assert.ok(!social.includes('localStorage'));console.log('HYVORA full workflow gate passed');
