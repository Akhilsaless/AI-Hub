import fs from'node:fs';import assert from'node:assert/strict';const r=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),'utf8'),core=r('functions/lib/ai-provider-core.js'),gateway=r('functions/api/providers/gateway.js'),models=r('functions/api/providers/models.js'),ui=r('Public/ai-providers.html'),vault=r('functions/lib/vault.js'),hope=r('functions/lib/hope-gateway.js');for(const p of ['gemini','groq','openrouter','cloudflare','mistral','huggingface','openai'])assert.ok(core.includes(`${p}:`),`missing provider ${p}`);for(const table of ['ai_provider_credentials','ai_model_registry','ai_gateway_settings','ai_gateway_usage'])assert.ok(core.includes(table),`missing ${table}`);assert.ok(core.includes('discoverProviderModels'));assert.ok(gateway.includes("'auto','fastest','free-first','best-quality','manual'"));assert.ok(gateway.includes('allow_paid_fallback'));assert.ok(models.includes('requireOwner'));assert.ok(ui.includes('Free Models Only'));assert.ok(ui.includes('Allow Paid AI Fallback'));assert.ok(ui.includes('Test Connection'));assert.ok(ui.includes('Discover Models'));assert.ok(vault.includes('AES-GCM'));assert.ok(hope.includes('executeZeroCost'));for(const bad of ['localStorage.setItem("api','console.log(apiKey)','key_cipher FROM integrations ORDER BY provider,key_cipher'])assert.ok(!ui.includes(bad));console.log('PASS ai-provider-gateway-gate: provider registry, encrypted credentials, discovery, policy controls and owner-only gateway UI verified');