import assert from 'node:assert/strict';import fs from 'node:fs';const ui=fs.readFileSync(new URL('../Public/academy-v2.js',import.meta.url),'utf8'),state=fs.readFileSync(new URL('../functions/api/academy/state.js',import.meta.url),'utf8');for(const token of ['YOUR LEARNING PATH','modulesComplete','Capstone required','Practice & evidence','reflection','verification','estimatedMinutes','Submit evidence & mastery','Evaluate my work','AI PLAYGROUND'])assert.ok(ui.includes(token),`academy UI missing ${token}`);for(const token of ['CURRICULUM','moduleStatus','levelReadiness','modules,readiness','estimatedMinutes','capstone'])assert.ok(state.includes(token),`academy state missing ${token}`);assert.ok(!ui.includes('Submit my completed work'),'obsolete self-claim completion shortcut must stay removed');assert.ok(!ui.includes("academyFinish('${id}',95)"),'old one-click 95 mastery shortcut must be removed');console.log('academy learner UI depth gate: PASS');