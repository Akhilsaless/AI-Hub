import assert from 'node:assert/strict';import fs from 'node:fs';import {newsToMicroLesson} from '../functions/lib/academy-tools-explorer.js';
const explore=fs.readFileSync('functions/api/academy/explore.js','utf8'),updates=fs.readFileSync('functions/api/academy/updates.js','utf8'),ui=fs.readFileSync('Public/academy-explorer.js','utf8');
assert.ok(explore.includes('academy_ai_updates'));assert.ok(explore.includes('verified-source-only'));assert.ok(updates.includes("sourceRequired:true"));assert.ok(updates.includes('coreProgressionImpact:false'));assert.ok(ui.includes('What’s New in AI'));assert.ok(ui.includes('Open official source'));const m=newsToMicroLesson({id:'verified-1',title:'Verified update',whyItMatters:'Changes workflow'});assert.equal(m.requiredForProgression,false);assert.equal(m.sourceRequired,true);console.log(JSON.stringify({ok:true,verifiedFeed:true,optionalMicroLessons:true,sourceLinks:true}));
